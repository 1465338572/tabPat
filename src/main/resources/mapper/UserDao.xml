<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.tabpat.dao.UserDao">
    <select id="selectList" resultType="UserDo">
        SELECT user_id,
               username,
               password,
               qq,
               we_chat,
               email,
               name,
               create_time,
               update_time,
               birth_day,
               phone,
               photo
        FROM users ${ew.customSqlSegment}
    </select>
    <select id="getUserByName" resultType="UserDo">
        SELECT *
        FROM users
        WHERE username = #{username};
    </select>
    <select id="getUserList" resultType="UserDo">
        SELECT user_id, username
        FROM users
    </select>

    <insert id="insert" parameterType="UserDo" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users(user_id,
                          username,
                          password,
                          qq,
                          we_chat,
                          email,
                          name,
                          create_time,
                          update_time,
                          birth_day,
                          phone,
                          photo)
        VALUES (#{userId},
                #{username},
                #{password},
                #{qq},
                #{weChat},
                #{email},
                #{name},
                #{createTime},
                #{updateTime},
                #{birthDay},
                #{phone},
                #{photo})
    </insert>

    <update id="updateById" parameterType="UserDo">
        UPDATE users
        <set>
            password = #{et.password},
            qq = #{et.qq},
            we_chat = #{et.weChat},
            email = #{et.email},
            name = #{et.name},
            update_time = #{et.updateTime},
            birth_day = #{et.birthDay},
            phone = #{et.phone},
            photo = #{et.photo}
        </set>
        WHERE username = #{et.username}
    </update>
</mapper>
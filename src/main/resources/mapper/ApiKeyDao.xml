<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tabpat.dao.ApiKeyDao">

    <select id="selectById" resultType="com.example.tabpat.domain.ApiKeyDo">
        SELECT *
        FROM api_key
        WHERE id = #{id};
    </select>
</mapper>